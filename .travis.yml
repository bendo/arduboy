 language: python
 python:
     - "2.7"

 sudo: false
 cache:
     directories:
         - "~/.platformio"

 env:
     - PLATFORMIO_CI_SRC=arduboy/buttons/src/main.ino
     - PLATFORMIO_CI_SRC=arduboy/bin2dec/src/main.ino

 install:
     - pip install -U platformio
     - platformio lib install "Arduboy"

 script:
     - platformio ci --project-conf=arduboy/buttons/platformio.ini
